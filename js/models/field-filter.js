/*global define*/
define([
    'jquery',
    'underscore',
    'backbone'
], function ($, _, Backbone) {
    'use strict';

    var FieldFilterModel = Backbone.Model.extend({

    });

    return FieldFilterModel;
});