/*global define*/
define([
    'jquery',
    'underscore',
    'backbone'
], function ($, _, Backbone) {
    'use strict';

    var SearchTermFilterModel = Backbone.Model.extend({

    });

    return SearchTermFilterModel;
});